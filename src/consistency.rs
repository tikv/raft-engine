

/// Returns a list of tuple containing the id of corrupted Raft group and its last
/// unaffected log index.
pub consistency_check() -> Vec<(u64, u64)> {
    todo!()
}

pub unsafe_remove_raft_groups(raft_groups: Vec<u64>) {
    todo!()
}
